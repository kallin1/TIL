# Time Complexity for Coding Test

## 입력 크기별 기준
- N ≤ 1e7 → O(n) 가능
- N ≤ 1e5 → O(n log n) 가능
- N ≤ 1e3 → O(n^2) 가능

## 자주 쓰는 기준
- 정렬: O(n log n)
- 이분탐색: O(log n)
- DFS/BFS: O(V + E)

## 판단 팁
- 이중 for문 보이면 → N 체크 먼저
- 시간초과 나면 → 자료구조/탐색 방식 변경
